<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Likes

    <a data-toggle="collapse" href="#trip_detail_likes_new_form">
      Add like
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @trip_detail.likes.each do |like| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <a href="/likes/<%= like.id %>">
          <%= like.detail_id %>
        </a>

        <a href="/delete_like_from_detail/<%= like.id %>">
          Delete
        </a>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="trip_detail_likes_new_form">
        <%= render partial: "trip_detail_templates/trip_detail_likes_new_form" %>
      </div>
    </div>

  </ul>
</div>
