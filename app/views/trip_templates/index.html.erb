<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All trips
    </h1>

    <a href="/trips/new" class="btn btn-block btn-outline-secondary">
      Add a new trip
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Name
        </th>

        <th>
          User
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @trips.each do |trip| %>
      <tr>
        <!-- Display trip.id start -->
        <td>
          <%= trip.id %>
        </td>
        <!-- Display trip.id end -->

        <!-- Display trip.name start -->
        <td>
          <%= trip.name %>
        </td>
        <!-- Display trip.name end -->

        <!-- Display trip.user_id start -->
        <td>
          <% if trip.user.present? %>
            <a href="/users/<%= trip.user.id %>">
              <%= trip.user.email %>
            </a>
          <% end %>
        </td>
        <!-- Display trip.user_id end -->

        <!-- Display trip.created_at start -->
        <td>
          <%= time_ago_in_words(trip.created_at) %> ago
        </td>
        <!-- Display trip.created_at end -->

        <!-- Display trip.updated_at start -->
        <td>
          <%= time_ago_in_words(trip.updated_at) %> ago
        </td>
        <!-- Display trip.updated_at end -->

        <td>
          <a href="/trips/<%= trip.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @trips, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
